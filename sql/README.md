This task aims to validate the Direction and Total Volume of trading symbols (e.g. EURUSD) by aggregating order data from multiple Liquidity Providers (LPs).

The validation ensures:

Total volume is calculated correctly

Direction is derived based on net volume

BUY volumes are treated as positive values

SELL volumes are treated as negative values